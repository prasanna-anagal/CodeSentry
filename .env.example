PORT=9000
MONGO_URI=mongodb://<user>:<password>@<cluster-hosts>/codesentry?ssl=true&replicaSet=<name>&authSource=admin

# Standard Redis protocol connection (for BullMQ / ioredis), NOT the Upstash REST API credentials
REDIS_URL=rediss://default:<password>@<endpoint>:<port>
